name: Deno Deploy

on:
  push:
  workflow_dispatch:

jobs:
  deploy:
    uses: ubiquity/deno-deploy-workflow/.github/workflows/deno-deploy-reusable.yml@4519d35
    with:
      project: notifications-ubq-fi
      preview_project: notifications-ubq-fi-pre
      entrypoint: serve.ts
      bun_version: latest
      install_command: bun install
      build_command: bun run build
      build_env: |
        SUPABASE_URL=$SUPABASE_URL
        SUPABASE_ANON_KEY=$SUPABASE_ANON_KEY
      include: |
        static/dist/**
    secrets: inherit
